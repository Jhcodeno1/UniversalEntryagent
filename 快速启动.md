# 意图路由 Agent 快速启动

## 1. 进入项目

```bat
cd /d D:\hundsun\公司agent项目\万能入口agent
```

## 2. 首次安装环境

如果项目里已经带有 `.venv`，可跳过本节。

```bat
py -3.10 -m venv .venv
.venv\Scripts\python.exe -m pip install --upgrade pip setuptools wheel
.venv\Scripts\python.exe -m pip install -r requirements-demo.txt
.venv\Scripts\python.exe -m pip install -e . --no-deps
```

如果目标机器没有 `py -3.10`，改用已安装的 Python 3.10 路径：

```bat
C:\Path\To\Python310\python.exe -m venv .venv
```

## 3. 复用已有 QwenPaw Python

如果目标机器已经有 QwenPaw Python 环境，也可以不创建 `.venv`，直接指定启动用的 Python：

```bat
set PYTHON_EXE=C:\Path\To\QwenPaw\python.exe
```

如需在该环境中补装本项目：

```bat
%PYTHON_EXE% -m pip install -r requirements-demo.txt
%PYTHON_EXE% -m pip install -e . --no-deps
```

## 4. 配置模型 Key

如果模型服务不校验 Key，可填占位值。

```bat
set LLM_API_KEY=empty
```

模型地址和模型名在 `config.json` 中修改：

```text
llm.base_url
llm.model
```

## 5. 构建前端

前端页面有修改时执行；如果 `frontend/dist` 已存在且不需要更新，可跳过。

```bat
cd /d D:\hundsun\公司agent项目\万能入口agent\frontend
cmd /c npm.cmd run build
```

## 6. 启动 Web Demo

```bat
cd /d D:\hundsun\公司agent项目\万能入口agent
scripts\web.bat 127.0.0.1 8765
```

浏览器访问：

```text
http://127.0.0.1:8765/demo
```

## 7. 快速验证

```bat
.venv\Scripts\python.exe -m pip check
.venv\Scripts\python.exe -m universal_entry_agent --help
```

如果使用 `PYTHON_EXE` 复用 QwenPaw 环境：

```bat
%PYTHON_EXE% -m pip check
%PYTHON_EXE% -m universal_entry_agent --help
```

## 8. 常用诊断

查看最近一次请求耗时：

```bat
.venv\Scripts\python.exe scripts\analyze_performance.py
```

列出最近 10 次请求：

```bat
.venv\Scripts\python.exe scripts\analyze_performance.py --list 10
```
